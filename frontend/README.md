# Frontend — Ledger (Agentic Financial Advisor)

React + TypeScript + Vite + Tailwind CSS client. See the repo-root `README.md`
for full setup instructions (Docker and non-Docker).

Quick start:
```bash
npm install
cp .env.example .env
npm run dev
```
